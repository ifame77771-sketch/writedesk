import {NextResponse} from 'next/server'; import {db} from '@/lib/db'; import {getSessionUserId} from '@/lib/auth';
async function owned(id:string){const uid=await getSessionUserId(); if(!uid)return null; return db.document.findFirst({where:{id,ownerId:uid}})}
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params; const d=await owned(id); if(!d)return NextResponse.json({error:'Not found'},{status:404}); return NextResponse.json({document:d})}
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params; const d=await owned(id); if(!d)return NextResponse.json({error:'Not found'},{status:404}); const b=await req.json(); const updated=await db.document.update({where:{id},data:{...(typeof b.title==='string'?{title:b.title.slice(0,200)}:{}),...(typeof b.content==='string'?{content:b.content}:{})}}); return NextResponse.json({document:updated})}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params; const d=await owned(id); if(!d)return NextResponse.json({error:'Not found'},{status:404}); await db.document.delete({where:{id}}); return NextResponse.json({ok:true})}
